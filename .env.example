# 环境配置示例文件
# 复制此文件为 .env 并填入实际值：cp .env.example .env
# 重要：.env 文件中的 NEXT_PUBLIC_* 变量会在构建时被打包到静态文件中

# ============================================
# 应用环境模式
# ============================================
# development - 开发模式，使用 localhost:3000
# production - 生产模式，使用静态导出文件
# export - 静态导出模式
# browser - 浏览器模式
ALAYE_APP_ENV=export

# ============================================
# Umami Analytics 配置
# ============================================
# 1. 在 Umami Cloud 创建项目：https://cloud.umami.is
# 2. 获取网站 ID from: Settings > Websites > Add website
# 3. 填入下方（留空则完全禁用分析功能）
#
# 注意：
# - 这些值会在构建时被嵌入到静态文件中
# - 用户需要在设置页面手动启用数据采集（默认关闭）
# - Website ID 是公开的，这是正常的设计
NEXT_PUBLIC_UMAMI_COLLECT_URL=https://cloud.umami.is/api/collect
NEXT_PUBLIC_UMAMI_WEBSITE_ID=

# ============================================
# Next.js 环境变量（自动读取）
# ============================================
# 注意：APP_NAME 和 APP_VERSION 会在构建时自动从 frontend/package.json 读取
# 通常不需要手动设置，除非你想覆盖
NEXT_PUBLIC_APP_NAME=Alaye App
NEXT_PUBLIC_APP_VERSION=0.1.2

# ============================================
# 开发服务器端口
# ============================================
PORT=3000
